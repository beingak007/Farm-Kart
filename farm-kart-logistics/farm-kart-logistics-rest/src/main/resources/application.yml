server:
  port: 8083
  servlet:
    context-path: /logistics-service

spring:
  application:
    name: logistics-service
  datasource:
    url: ${LOGISTICS_DB_URL:jdbc:mysql://localhost:3306/farmkart_logistics?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${LOGISTICS_DB_USER:farmkart}
    password: ${LOGISTICS_DB_PASS:farmkart}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    baseline-on-migrate: true
    validate-on-migrate: false
    out-of-order: true
    locations: classpath:db/migration/logistics
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
