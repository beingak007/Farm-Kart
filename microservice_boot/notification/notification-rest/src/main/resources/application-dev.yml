spring:
  config:
    import: optional:classpath:config/application-dev-common.yml
  jpa:
    show-sql: true
  kafka:
    consumer:
      group-id: farmkart-notification-dev

farmkart:
  sms:
    provider: console
