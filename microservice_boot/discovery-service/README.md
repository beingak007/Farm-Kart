# Discovery Service — Netflix Eureka registry
#
# Port: 8084 (dashboard: http://localhost:8084)
# Clients register with: http://localhost:8084/eureka
#
# Build:  cd microservice_boot && mvn -pl discovery-service/discovery-service-rest -am package
# Run:    java -jar discovery-service/discovery-service-rest/target/discovery-service-rest-*.jar
