spring:
  config:
    import: optional:classpath:config/application-prod-common.yml
  jpa:
    show-sql: false
  datasource:
    hikari:
      maximum-pool-size: 10
  flyway:
    validate-on-migrate: true
    out-of-order: false
  kafka:
    consumer:
      group-id: farmkart-agent

farmkart:
  agent:
    llm:
      openai:
        api-key: ${OPENAI_API_KEY}
        model: ${OPENAI_MODEL:gpt-4o}
  services:
    farmer-url: ${FARMKART_APP_URL}
    buyer-url: ${FARMKART_APP_URL}
    logistics-url: ${FARMKART_APP_URL}
    warehouse-url: ${FARMKART_APP_URL}
    market-price-url: ${FARMKART_APP_URL}
    catalog-url: ${FARMKART_APP_URL}
    ai-advisory-url: ${FARMKART_APP_URL}
    reporting-url: ${FARMKART_APP_URL}
